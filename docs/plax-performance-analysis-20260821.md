# PLAX 性能复测分析（2026-08-21）

## 输入证据

- `D:\heart-data\validation\perf-plax-20260821-084417\full-task-summary.json`
- `D:\heart-data\validation\perf-plax-20260821-084417\full-task-process-tree.jsonl`

## 结论

任务最终为 `completed`，单图错误为空，算法版本为
`heart@bundle-f06f5f050c98+models@4bf76612484dd393`。摘要记录的监控覆盖耗时为
768.142 秒，峰值进程树 RSS 为 1361.17 MB。

该 JSONL 不是一次运行的独立文件，而是包含 4 段数据；`elapsedSeconds` 分别在第
87、2043、2061 条记录处回退。最新有效段为第 2061 条至文件末尾，共 711 条，
覆盖 767.027 秒并到达 `completed`。因此不能把整个 JSONL 直接汇总为一次运行。

最新段监控开始时，第一个推理子进程已经累计约 84 秒 CPU 时间。后续子进程切换点为：

| 子进程 | 有效观测区间（秒） | 有效观测墙钟（秒） |
| --- | ---: | ---: |
| 1（已运行后开始监控） | 0.101–68.354 | 68.253 |
| 2 | 69.432–208.809 | 139.377 |
| 3 | 209.878–349.032 | 139.154 |
| 4 | 350.117–489.276 | 139.159 |
| 5 | 490.350–627.180 | 136.830 |
| 6 | 628.263–763.924 | 135.661 |

由于首模型已有约 84 秒 CPU 未被墙钟覆盖，提交到完成的真实耗时下限约为
852 秒（约 14 分 12 秒），而不是 768 秒。相较既有约 2489 秒基线，耗时下降约
66%，但仍未达到 10 分钟阶段目标。

后五个模型的 CPU 时间增长与墙钟时间几乎相等，说明仍是接近单逻辑核心的 CPU
密集计算。六个模型仍由不同子进程顺序执行；本次改善不是持久 Worker 或输入复用带来的。
各模型耗时已经收敛到约 136–140 秒，AorticRoot 不再表现为独有长尾。

本次目录没有 `plax-stage-timings.json`，说明新阶段计时代码尚未进入该服务器运行包，
当前证据仍无法区分模型前向、加载、DICOM 处理和视频后处理的占比。下一次复测必须：

1. 部署带 `--timings_path` 的新 Measurement 脚本和服务端参数。
2. 每次运行创建全新 JSONL，使用独占创建，禁止追加。
3. 在任务提交前启动监控，记录提交、开始、完成时间。
4. 冷启动一次、热启动至少三次后，再按阶段占比决定持久 Worker 或 GPU 门禁。

