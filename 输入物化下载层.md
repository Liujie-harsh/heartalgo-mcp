# 输入物化下载层

算法服务在 `CombinedRunner` 分流和申请 GPU 之前，将 HTTP/HTTPS 输入下载到任务隔离目录，再把本地路径交给现有心超和 ECG runner。原始 `dcmPath` / `ecgPath` 不会被覆盖，MySQL 与 `/result` 仍保存和返回调用方提交的引用。

## 当前输入契约

- `dcmPath` / `ecgPath` 为 `http://` 或 `https://` 时执行下载。
- 其他值继续按原有本地文件路径处理，便于服务器联调和回滚。
- 当前远程引用必须是可直接下载的稳定 URL；尚未定义 `fileId + 固定文件服务` 协议。
- 不应提交带签名查询参数的短期 URL，因为原始引用需要用于任务恢复、持久化和结果回显。

下载目录：

```text
<TASK_WORK_ROOT>/<安全taskId>-<摘要>/inputs/cardiac_ultrasound/<安全dcmId>-<摘要>.dcm
<TASK_WORK_ROOT>/<安全taskId>-<摘要>/inputs/ecg/<安全ecgId>-<摘要>.xml
```

所有 taskId 和输入 ID 都会附加其 SHA-256 摘要片段，避免不同 ID 清洗后碰撞。

## 环境配置

```powershell
$env:HTTP_INPUT_ALLOWED_HOSTS = "files.internal.example:8443,object.internal.example"
$env:HTTP_INPUT_MAX_BYTES = "536870912"
$env:HTTP_INPUT_TIMEOUT_SECONDS = "60"
$env:HTTP_INPUT_BEARER_TOKEN = "<通过受保护环境注入的服务凭据>"
```

| 变量 | 默认值 | 说明 |
|---|---:|---|
| `HTTP_INPUT_ALLOWED_HOSTS` | 空 | 精确 `host` / `host:port` 白名单；为空时远程下载关闭 |
| `HTTP_INPUT_MAX_BYTES` | 536870912 | 单文件最大字节数，同时校验响应头和实际读取量 |
| `HTTP_INPUT_TIMEOUT_SECONDS` | 60 | 单次请求 socket 超时秒数 |
| `HTTP_INPUT_BEARER_TOKEN` | 空 | 可选服务间 Bearer Token；禁止写入源码或普通配置文件 |

当前允许 HTTP/HTTPS 重定向；白名单只校验调用方提交的初始 URL，重定向目标不再校验白名单，以兼容内网文件服务跳转到实际存储地址。跨主机或跨协议跳转时不会转发 Bearer Token。下载先写 `.part`，通过大小和完整性检查后再原子替换为正式输入文件，失败或断流会删除 `.part`。

## 失败语义

- 单张心超下载失败：仅该输入产生错误，其余心超继续推理。
- ECG 下载失败：任务整体失败，与现有 ECG 子任务契约一致。
- 目录创建、缓存检查和文件写入错误统一转换为输入物化错误，避免意外破坏单张心超隔离。

## 后端确认后再决定

以下内容尚未固化到接口协议：稳定 URL 还是 `fileId`、下载接口鉴权方式、正式文件大小上限、文件不可变校验（SHA-256/ETag）和下载文件保留期限。若采用 `fileId`，应新增可注入的引用解析器，将其转换为白名单 URL，下载和 runner 层无需改变。
